#include "main.h"
/**
 * _abs - Computes the absolute of an integer
 * @n: the number to be converted
 * Return: The absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n)
}
